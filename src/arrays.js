/** List age of the users  
 * @type{Array}
 */
const age = [4,14,24,41,45,]

/**
 *  array of users scores
 * @type{Array<Number>}
 */
const puntajes = [ 10.1,55.55,5.6]

/** array of users scores
 * @type{Array<Number | String >}
 */
const scoresAndStrings = [10.10,"julio",]

/** array of users scores
 * @type{Array<String >}
 */
const names = ["emily", "Valeria", "adriana "]

names.push("Cesar");